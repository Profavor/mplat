spring.jpa.hibernate.ddl-auto=update

spring.datasource.url=jdbc:mysql://dkseo.synology.me:3307/profavor?charset=utf8
spring.datasource.username=profavor
spring.datasource.password=profavor!
spring.datasource.driver-class-name=com.mysql.jdbc.Driver
spring.datasource.initialization-mode=never

# Number of ms to wait before throwing an exception if no connection is available.
spring.datasource.max-wait=10000

# Maximum number of active connections that can be allocated from this pool at the same time.
spring.datasource.max-active=50

# Validate the connection before borrowing it from the pool.
spring.datasource.test-on-borrow=true

spring.jpa.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.jpa.show-sql=false
spring.session.store-type=none

# App configuration
favor.token.header=x-auth-token
favor.token.secret=RmF2b3JTZWNyZXQhQA==
favor.token.expiration=604800

# Routes
favor.route.authentication=/api/auth/login
favor.route.authentication.refresh=/api/auth/refresh
favor.route.protected=protected

security.oauth2.client.client-id=favorPlatform
security.oauth2.client.client-secret=secretFavor
security.oauth2.resource.token-info-uri=http://localhost:7010/oauth/check_token

#Javers
javers.mapping-style=FIELD
javers.algorithm=SIMPLE
javers.pretty-print=true
javers.type-safe-values=false
javers.new-object-snapshot=false
javers.packages-to-scan=
javers.auditable-aspect-enabled=true
javers.spring-data-auditable-repository-aspect-enabled=true



